/**
 * This enum represents the different character types
 * that can be selected in the game.
 */
public enum Character {
    /** Default character type. */
    DEFAULT,

    /** Marine character type. */
    MARINE,

    /** Robot character type. */
    ROBOT
}